# 网站搭建与部署流程（纯 HTML 项目）

> 从「一段代码」到「全世界能访问的网址」，每一步是什么、为什么、怎么做。以本项目（单文件 HTML 转盘）为实例，适配自 GTA6 情报站那套 Next.js 部署经验，**去掉**了 React/Next/Markdown/i18n 等本项目用不到的部分，只留通用流程。

---

## 0. 一个网站的本质

网站本质是**放在服务器上的一堆文件**。浏览器打开网址时：服务器把 HTML 文件传回来 → 浏览器渲染成页面。

- **HTML** = 骨架与内容
- **CSS** = 样式（颜色、布局）
- **JS** = 行为（点击、转盘动画、调接口）

本项目三个都齐了，而且**全在一个文件里**。这就是最简单的静态网站。

**静态 vs 动态**：

- **静态网站**：服务器上存着现成的 HTML，谁来访问都返回同一个文件。快、便宜、稳。
- **动态网站**：服务器收到请求后现场"算"出 HTML（需要一直运行的服务器 + 数据库）。

本项目选静态：转盘逻辑全在浏览器里跑，服务器只需要把那个 HTML 文件吐出去就行。

---

## 1. 第一步：把代码变成能打开的网页

现在代码叫 `轮盘代码.md.txt`，浏览器不认 `.txt`。**改成 `index.html`**（`index` 是约定俗成的首页文件名，服务器/托管平台看到 `index.html` 就自动当首页）。

改完双击打开，或建个文件夹放进去用 VS Code 的 Live Server 预览。此时：

- 能正常走「输入地址 → 选餐厅 → 转盘 → 结果」全流程（用的 Mock 演示数据）
- 数据源还是假的，`DATA_SOURCE = 'mock'`

到这一步，你已经有一个**本地能跑**的网站了。剩下的问题只是：怎么让别人也能访问。

---

## 2. 部署前：放进 Git 仓库

部署平台（Cloudflare Pages）通过连你的 **GitHub 仓库**自动部署。所以先建仓：

1. 在 GitHub 建一个新仓库（比如 `restaurant-wheel`）
2. 本地 `git init` → `git add .` → `git commit` → 推到 GitHub
3. 仓库根目录放 `index.html`（其他文件如 README、文档可以一起放，不影响）

**Git 是什么**：版本控制，记录每次改动、能回退。`commit` = 本地存档，`push` = 传到 GitHub。后面每次 `push` 都会自动触发部署。

---

## 3. 部署到 Cloudflare Pages（纯静态）

Cloudflare Pages 免费、全球 CDN、连 GitHub 自动构建。对纯 HTML 项目，配置极简：

| 项 | 值 |
|---|---|
| 连接 | 你的 GitHub 仓库，生产分支 `main` |
| 构建命令 (Build command) | **留空**（纯 HTML 不需要构建） |
| 输出目录 (Build output directory) | **留空 / 填 `/`**（文件就在仓库根目录） |
| 环境变量 | 无（**不用**设 `NODE_VERSION`，那是 Next.js 才需要的） |

创建后 Cloudflare 会给你一个临时域名 `xxx.pages.dev`，**立刻就能访问**。之后每次 `git push origin main`，它自动拉代码、部署到全球 CDN，全程约 1 分钟。

**和 GTA6 情报站的关键区别**：那个项目要 `Build command = npm run build`、`输出目录 = out`、`NODE_VERSION = 22`，因为它是 Next.js 要编译。**本项目是纯 HTML，这三样统统不用**。

---

## 4. 域名与 DNS（为什么域名能指向你的站）

临时域名 `xxx.pages.dev` 能用但不好记。要绑自己的域名：

1. 域名在 Cloudflare 上（或把 NS 迁到 Cloudflare，`Begin DNS transfer`）
2. Pages 后台 → Custom domains → 填你的域名 → 激活
3. 域名已在 Cloudflare 时，Cloudflare 会自动接管路由，**不用手动加 CNAME**，HTTPS 证书也自动签发

**DNS 是什么**：域名（人记的名字）→ IP（服务器地址）的翻译系统。常见记录：

| 记录 | 作用 |
|---|---|
| A | 域名 → IP |
| CNAME | 域名 → 另一个域名 |
| NS | 指定这个域名归哪家 DNS 管 |
| TXT | 放文本（验证域名所有权用） |

---

## 5. 两个踩过的坑（和框架无关，纯 HTML 一样会遇到）

这两个坑是 GTA6 情报站上线时实打实踩过的，**纯 HTML 项目同样适用**：

### 坑 1：域名显示 active，但解析不生效

- **现象**：Pages 后台自定义域名显示 `active`、证书也签发了，但 `curl` 你的域名报 `Could not resolve host`，或 Google/Bing 提交 sitemap 报「无法抓取」。
- **原因**：Cloudflare 有时只显示了 active，但 DNS 解析记录没真正落库（裸域没有 A/AAAA/CNAME 记录）。
- **修复**：把该域名从 Pages「摘掉再重挂」（删掉自定义域名再重新添加），触发 Cloudflare 重写解析记录，约 20 秒生效。

### 坑 2：Cloudflare 默认挡 AI 爬虫

- **现象**：`robots.txt` 被 Cloudflare 自动注入 `Disallow: GPTBot / ClaudeBot / Google-Extended` 等，AI 爬虫（ChatGPT、Perplexity）抓不到你的站。普通 Google 搜索不受影响，所以看起来"一切正常"。
- **修复**：dashboard → 域名 → **AI Crawl Control**（或 Security → Bots）→ 关「Managed robots.txt」开关，再把「Block AI training bots」设为「Do not block (allow crawlers)」。
- **要不要管**：如果你不在乎被 AI 引用，这个坑可以忽略；想被 AI 引用才需要关。

---

## 6. 日常发布流程（上线后）

改代码 → 本地浏览器验证 → `git add -A && git commit -m "..."` → `git push origin main` → Cloudflare 自动部署。就这么简单。

---

## 附：完整链路一图流

```
改代码（index.html）
      ↓
本地浏览器打开验证
      ↓
git add + commit（存档）
      ↓
git push origin main（传 GitHub）
      ↓
Cloudflare Pages 检测到 push → 自动部署（纯静态，无需构建）
      ↓
用户访问你的域名 → DNS 解析到 Cloudflare → CDN 返回 index.html
```
