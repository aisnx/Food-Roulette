# 代码完善清单

> 最后更新：2026-09-17。当前工作版本 `index.html` 已完成全部 Mock 功能，剩余「接真实数据」和「部署上线」两件事。数据接入已定为「暂不接，先留 Mock」。

---

## 已完成 ✅

以下功能已实现，无需再动：

| 模块 | 说明 |
|---|---|
| 三视图流程 | 设置 → 列表 → 转盘 → 结果弹窗 |
| Mock 演示数据 | 36 家餐厅池，离线可跑通，同一条件结果稳定 |
| 自定义转盘 | 手输 / 批量导入，权重 ×1~×3 调抽中概率 |
| 黑名单 | 拉黑永久排除 + 当前轮立即排除 |
| 历史记录 | localStorage 持久化（最近 30 条） |
| 分享 | 复制文字 + Canvas 生成分享图 |
| 抽中后不再出现 | 设置里可开关 |
| 列表筛选/排序 | 营业中 / 评分≥4.5 / 人均 / 菜系，及距离 / 评分 / 人均排序 |
| AmapProvider 适配器 | `geocode` / `regeo` / `search` 均已写好 |
| 定位按钮 | 已接入逆地理编码（坐标 → 地址回填） |
| 细节收尾 | favicon、CSS 渐变封面（去外链）、系统字体（去飞书 CDN）、营业状态免责提示 |

---

## 待办（按优先级）

### 一、接入高德真实数据（暂缓，等有 Key 再定方案）

代码已预留接入点，届时只需两处：

1. `AMAP_KEY = '...'` —— 填真实 Key（`index.html` 第 559 行）
2. `DATA_SOURCE = 'mock'` → `'amap'`（第 560 行）

**⚠️ 安全提醒（别跳过）**：高德有两类 Key，用错会泄露：

| Key 类型 | 用途 | 放前端安全吗 |
|---|---|---|
| **Web服务 (Web Service)** | 服务器端 HTTP API（`restapi.amap.com/v3/...`） | ❌ 不安全，Key 会暴露 |
| **Web端 (JS API)** | 浏览器端 JS（配域名白名单） | ✅ 相对安全 |

当前代码用的是 Web服务 API。三个方案：

- **方案 A（改代码）**：用「JS API Key」+ `AMap.PlaceSearch`，配域名白名单。前端安全，搜索能力略弱。
- **方案 B（加代理）**：Cloudflare Worker / Pages Function 中转，Key 只在服务端，前端永不暴露。保留现有代码，改动最小。
- **方案 C（先本地玩）**：继续 Mock（**当前状态**）。

### 二、部署上线

1. 本地浏览器全流程点一遍（双击 `index.html`）
2. 建 GitHub 仓库、push（`git init` 已完成，`.gitignore` 已忽略旧文件 `轮盘代码`）
3. Cloudflare Pages 连仓库（构建命令留空、输出目录 `/`）
4. 绑域名（可选）

详见 [网站搭建与部署流程.md](./网站搭建与部署流程.md)。

### 三、可选优化（非必需）

| 项 | 现状 | 建议 |
|---|---|---|
| 高德类型过滤 | 已按 `extractCuisine` 粗分类 | 可再按菜系细分映射到前端 tag |
| PWA / 离线 | 无 | 想"加到桌面"可加 manifest + service worker |
| 列表名未转义 | `renderList` 里 `r.name` 直接 innerHTML | 接真实数据前建议对名称做 `escapeHtml`，防异常字符 |
